{
  "name": "Tastify",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-sass": "sass scss:public/css",
    "watch-sass": "sass --watch scss:public/css",
    "serve": "live-server --ignore='scss|node_modules'"
  },
  "private": true,
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.86.3"
  }
}
